{
  "enabled_groups": {
    "description": "启用 Groupmate 的群",
    "type": "list",
    "default": [],
    "hint": "填写 AstrBot 已接入平台中的群 ID。运行模式会应用到这里列出的群。"
  },
  "runtime_mode": {
    "description": "运行方式",
    "type": "string",
    "options": ["SHADOW", "SOCIAL_RUNTIME"],
    "labels": ["SHADOW 观察（不发送）", "正式运行（允许发送）"],
    "default": "SHADOW",
    "hint": "SHADOW 只观察并记录决策，不发送消息；正式运行允许 Groupmate 在已启用的群中参与对话。"
  },
  "persona_name": {
    "description": "正式名称",
    "type": "string",
    "default": "爱弥斯",
    "hint": "爱弥斯在群聊中的正式名称，也用于直接呼唤识别。"
  },
  "persona_aliases": {
    "description": "其他称呼",
    "type": "list",
    "default": ["小爱"],
    "hint": "群友可以用这些称呼呼唤爱弥斯，每行一个。"
  },
  "persona_preset": {
    "description": "人格资料预设",
    "type": "string",
    "options": ["aemeath_current", "custom"],
    "labels": ["爱弥斯（当前剧情）", "自定义 Persona"],
    "default": "aemeath_current",
    "hint": "预设只提供人格资料，不硬编码别称；名称和别称仍使用上方配置。",
    "invisible": true
  },
  "generation_provider": {
    "description": "知识检索所用 AstrBot Provider（可选）",
    "type": "string",
    "_special": "select_provider",
    "default": "",
    "hint": "仅知识联网检索仍走 AstrBot Provider。场景识别和回复生成已改为插件直连，不再使用此项。"
  },
  "cognition_api_key": {
    "description": "DeepSeek API Key",
    "type": "string",
    "default": "",
    "obvious_hint": true,
    "hint": "插件直连共用密钥，用于参与判断、场景识别和回复生成。请勿截图、导出或粘贴到日志中。"
  },
  "cognition_api_base": {
    "description": "DeepSeek API 地址",
    "type": "string",
    "default": "https://api.deepseek.com",
    "hint": "默认使用 DeepSeek 官方地址，一般无需修改。"
  },
  "cognition_model": {
    "description": "理解模型名称",
    "type": "string",
    "default": "deepseek-v4-flash",
    "hint": "用于参与判断和场景识别。关闭推理模式后通常 1 到 3 秒返回。"
  },
  "generation_model": {
    "description": "回复模型名称",
    "type": "string",
    "default": "deepseek-v4-flash",
    "hint": "用于最终回复和模仿确认。暂时与理解模型相同，后续可单独换成更快或更强的模型。"
  },
  "cognition_timeout_seconds": {
    "description": "理解模型等待时间",
    "type": "int",
    "default": 8,
    "slider": {"min": 3, "max": 15, "step": 1},
    "hint": "参与判断和场景识别的安全上限。超时后场景识别降级为确定性场景，回复链路继续。"
  },
  "generation_timeout_seconds": {
    "description": "回复模型等待时间",
    "type": "int",
    "default": 15,
    "slider": {"min": 5, "max": 60, "step": 1},
    "hint": "最终回复生成的安全上限。超时记为回复失败，不再无限等待。"
  },
  "profile_enabled": {
    "description": "持续学习群成员画像",
    "type": "bool",
    "default": true,
    "hint": "开启后只在后台批量提取有证据的成员信息，不阻塞群聊消息与回复。"
  },
  "profile_batch_messages": {
    "description": "画像后台批次消息数",
    "type": "int",
    "default": 20,
    "slider": {"min": 1, "max": 20, "step": 1},
    "hint": "累计到该数量时唤醒一次后台画像提取；单次最多处理 20 条。"
  },
  "profile_batch_interval_seconds": {
    "description": "画像后台处理间隔（秒）",
    "type": "int",
    "default": 600,
    "slider": {"min": 60, "max": 3600, "step": 60},
    "hint": "消息不足一个批次时的定时处理间隔，默认 10 分钟。"
  },
  "profile_timeout_seconds": {
    "description": "画像模型等待时间（秒）",
    "type": "int",
    "default": 30,
    "slider": {"min": 5, "max": 120, "step": 5},
    "hint": "仅影响后台画像提取；超时会安全重试，不影响群聊回复。"
  },
  "knowledge_enabled": {
    "description": "启用群聊游戏知识",
    "type": "bool",
    "default": true,
    "hint": "开启后加载五款预装游戏语义，并在后台学习当前群的称呼约定；关闭后不启动解析和观察服务。"
  },
  "knowledge_web_search_enabled": {
    "description": "允许知识联网检索",
    "type": "bool",
    "default": true,
    "hint": "为后续版本事实检索预留。本阶段只记录配置，不会发起任何网络请求。"
  },
  "sticker_enabled": {
    "description": "闲聊配表情图",
    "type": "bool",
    "default": true,
    "hint": "开启后，爱弥斯在写出闲聊回复后，会从图鉴里抽一张含义对得上的图配上。没有合适的图时仍然只发文字。"
  },
  "sticker_capture_enabled": {
    "description": "从群友消息里收表情",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。打开后，已启用 Groupmate 的群里，群友发的自定义 GIF/PNG/WebP 会进「待认知」，不会直接出现在图鉴。商城表情和系统黄豆不会收。"
  },
  "vision_provider": {
    "description": "Groupmate 视觉模型（可选）",
    "type": "string",
    "_special": "select_provider",
    "default": "",
    "hint": "选一个能看图的模型。配上后，待认知里的图会在后台先判定是不是表情，再写含义；不配则只能手写。闲聊发送时不会再调它。"
  },
  "external_command_prefixes": {
    "description": "外部插件指令归属",
    "type": "list",
    "default": [],
    "hint": "每行填写 指令前缀=插件标识，例如 bq=astrbot.meme。匹配后 Groupmate 只观察，不抢答。"
  },
  "external_link_domains": {
    "description": "外部插件链接归属",
    "type": "list",
    "default": [],
    "hint": "每行填写 域名=插件标识，例如 v.douyin.com=astrbot.video。匹配后交给对应插件处理。"
  }
}
